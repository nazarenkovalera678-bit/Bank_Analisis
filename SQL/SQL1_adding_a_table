## Початок роботи з SQL

На першому етапі роботи з базою даних була підготовлена та завантажена таблиця **Bank**.

Виконані наступні кроки:

1. **Створено таблицю в SQL**

   Була створена структура таблиці з відповідними назвами колонок та правильними типами даних для кожного поля (INT, VARCHAR, DECIMAL, BOOLEAN).

2. **Підготовлено та додано дані через INSERT INTO**

   Дані були попередньо підготовлені у форматі SQL-запитів `INSERT INTO` та додані до створеної таблиці.

3. **Перевірено успішне завантаження таблиці**

   Після виконання запитів таблиця **Bank** була успішно заповнена даними та готова до подальшого аналізу за допомогою SQL-запитів.
